# Copy to .env

# Must match ADMIN_TOKEN in backend-python/.env, or every create/update/delete
# and the mail send button will come back 401.
#
# This value is bundled into the client build, so treat it as a guard against
# casual access rather than as a user identity.
VITE_ADMIN_TOKEN=
